# Copy to ~/.config/openclaw/dispatch.env (or set OPENCLAW_DISPATCH_ENV)

# Repo root where /dispatch and /dispatchi project folders are created
REPOS_ROOT=/home/miniade/repos

# Local launch logs for slash wrappers
LAUNCH_LOG_DIR=/home/miniade/clawd/data/dispatch-launch

# Optional wrappers (preferred if installed)
CC_DISPATCH_BIN=/home/miniade/.local/bin/cc-dispatch
CC_DISPATCHI_BIN=/home/miniade/.local/bin/cc-dispatchi
CC_RALPH_CANCEL_BIN=/home/miniade/.local/bin/cc-ralph-cancel

# Fallback repo for headless dispatch when CC_DISPATCH_BIN is absent
DISPATCH_REPO=/home/miniade/repos/claude-code-dispatch

# Defaults
DISPATCH_PERMISSION_MODE=bypassPermissions
DISPATCHI_MAX_ITERATIONS=20
DISPATCHI_COMPLETION_PROMISE=COMPLETE

# Results base used by /cancel run-id lookup and dispatch fallback
RESULTS_BASE=/home/miniade/clawd/data/claude-code-results
